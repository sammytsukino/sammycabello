export function PostHeroSection() {
  return (
    <section
      id="post-hero"
      aria-label="Contenido principal"
      className="relative min-h-[120vh] bg-portfolio-pink [view-transition-name:post-hero]"
    >
      <span className="sr-only">Inicio del contenido bajo la portada.</span>
    </section>
  )
}
